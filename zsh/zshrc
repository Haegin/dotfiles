# # Uncomment this and the zprof call at the bottom to profile startup time
# zmodload zsh/zprof

# defined in ~/.zsh/zshenv
source_files_in_directory $ZDOTDIR/zshrc.d

[ -f ~/.zsh/zshrc.local ] && source ~/.zsh/zshrc.local
